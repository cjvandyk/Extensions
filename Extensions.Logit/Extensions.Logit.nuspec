<?xml version="1.0"?>
<package >
  <metadata>
    <id>GCCHigh.Extensions.Logit</id>
    <!--<version>6.0.800</version>-->
    <title>$title$</title>
    <authors>Cornelius J. van Dyk</authors>
    <owners>$author$</owners>
    <license type="expression">GPL-3.0-or-later</license>
    <projectUrl>https://github.com/cjvandyk/Extensions</projectUrl>
    <iconUrl>https://raw.githubusercontent.com/cjvandyk/Extensions/master/Images/Extensions-64x64.png</iconUrl>
    <requireLicenseAcceptance>True</requireLicenseAcceptance>
    <description>
		GCCHigh.Extensions contains extension methods to make logging to console, file, iLogger, EventLog, SharePoint list, and database a breeze, specifically targeted to the GCCHigh M365 environment.
		GCCHigh.Extensions supports all versions of.NET from 4.6.2 through 8.0.
		Highlights are methods such as:
		Use .Inf() to write Information messages.
		Use .Wrn() to write Warning messages.
		Use .Err() to write Error messages.
		Use .Vrb() to write Verbose messages.
	</description>
    <summary>
		GCCHigh.Extensions contains extension methods to make logging to console, file, iLogger, EventLog, SharePoint list, and database a breeze, specifically targeted to the GCCHigh M365 environment.
		GCCHigh.Extensions supports all versions of.NET from 4.6.2 through 8.0.
		Highlights are methods such as:
		Use .Inf() to write Information messages.
		Use .Wrn() to write Warning messages.
		Use .Err() to write Error messages.
		Use .Vrb() to write Verbose messages.
	</summary>
    <releaseNotes>
		GCCHigh.Extensions contains extension methods to make logging to console, file, iLogger, EventLog, SharePoint list, and database a breeze, specifically targeted to the GCCHigh M365 environment.
		GCCHigh.Extensions supports all versions of.NET from 4.6.2 through 8.0.
		
		Err()
		Write an Error message to active channels (console, event log, file,
		SharePoint list, database) using the System.Logit class.

		GetExecutingAssembly()
		Gets the current Entry or Executing assembly through reflection.

		GetFQDN()
		Get the current computer Fully Qualified Domain Name.

		Inf()
		Write an Information message to active channels (console, event log, file,
		SharePoint list, database) using the System.Logit class.

		TimeStamp()
		Returns a string representing the current local date time stamp to
		either just the day or down to the millisecond.  Used for creating
		unique log file names.
		For example:
		TimeStamp()
		will return
		2021-03-01@06.01.02.003
		whereas
		TimeStamp(true)
		will return
		2021-03-01

		Vrb()
		Write a Verbose message to active channels (console, event log, file,
		SharePoint list, database) using the System.Logit class.

		Wrn()
		Write a Warning message to active channels (console, event log, file,
		SharePoint list, database) using the System.Logit class.
	</releaseNotes>
    <copyright>$copyright$</copyright>
    <tags>GCCHigh Extensions C# cjvandyk Crayveon  GCCH CSharp Logit Logging Logger iLogger EventLog</tags>
  </metadata>
  <files>
    <file src="**" target="lib" />
  </files>
</package>